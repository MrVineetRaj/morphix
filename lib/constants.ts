export const VideoStatuses = {
  DRAFT: "draft",
  PROCESSING: "processing",
  COMPLETED: "completed",
  FAILED: "failed",
};

export const availableVideoStatuses = Object.values(VideoStatuses);


export const VideoType = {
  PUBLIC: "public",
  PRIVATE: "private",
}

export const availableVideoTypes = Object.values(VideoType);